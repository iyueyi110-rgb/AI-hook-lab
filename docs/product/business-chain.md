# 业务链

```text
主题与平台输入 → 白名单和长度校验 → Prompt 版本选择 → 多版生成
→ JSON 解析和字段归一 → 模型自评分排序 → 收藏/复制/采用
→ 人工满意度与 Bad Case → 来源隔离事件 → baseline/candidate 复盘 → 下一轮约束
```

模型只提供候选内容。是否采用、发布和修改 Prompt 由人决定；历史、收藏、人工反馈和评测集不能被模型输出覆盖。
