# Demo 验证记录

Demo：<https://hookovo.icu/>

当前公开入口决定：暂不在 README 提供在线访问链接。2026-07-27 的自动公网请求已通过，但本轮手机流量、无痕浏览器与桌面交互人工验活均未完成；只有三项人工验收全部通过后，才能恢复为公开入口。

## 2026-08-06 本地主路径复核

本次复核基于从远程 `main` 恢复的有效工作树及尚未部署的本地修改，不代表线上环境已经更新。

| 检查 | 结果 | 证据边界 |
| --- | --- | --- |
| 无 Key 经典生成 | 返回 503，并显示“生成服务暂不可用” | 未调用真实模型，只验证安全错误态 |
| 错误信息 | 不显示 `.env.local`、Key 名称或模型服务状态码 | 只验证当前经典生成映射 |
| 重试恢复 | 主题、平台和内容类型保持不变 | 不代表模型调用成功 |
| 390px 移动端 | 页面宽度等于视口宽度，无横向溢出 | 使用桌面浏览器移动视口，不替代真实手机流量 |
| 1440px 桌面端 | 页面宽度等于视口宽度，无横向溢出 | 本地开发环境验证 |
| 线上部署一致性 | 待验证 | 当前修改尚未部署，不恢复公开入口 |

## 2026-08-06 阶段五完整流程验收

本轮使用系统临时目录中的隔离 JSON 数据，在真实浏览器中完成首次初始化、账号治理、Mock 批次、三类角色和报告门禁验收。Mock 评分只用于触发产品状态机，不属于真实研究数据，也没有写入仓库业务数据。

| 检查 | 结果 | 证据边界 |
| --- | --- | --- |
| 首次管理员初始化 | 通过；修复 Next 开发代理下合法同源请求被误拒绝的问题 | 同源校验仍拒绝跨域主机、协议降级和非法 Origin |
| 完整 Mock 批次 | 120/120 生成任务、360/360 候选、120/120 正式结果、240/240 匿名评分、60/60 定案 | 批量重复评分为流程夹具，不代表人工偏好或产品效果 |
| 评测员权限 | 只显示匿名 A/B 方案和本人任务，完成态计数为 60/60 | 不显示 Baseline/Candidate 映射或管理员能力 |
| 裁决员权限 | 显示 0 个待裁决分歧 | 只验证无分歧路径；分歧裁决规则由自动化测试覆盖 |
| Mock 报告 | 完成态仍为“需要继续评测”，七项门禁均为“待验证” | 100% Mock 评分不能形成升级结论 |
| 七类导出 | 5 个 CSV、1 个 JSON、1 个 Markdown 均返回 200 | 导出内容来自隔离 Mock 批次 |
| 异常埋点 | 无 Key 生成返回 503；`generation_error` 事件修复后返回 200 | 只记录受控错误类别，不记录主题或内部配置 |
| 1440px 与 390px | 页面宽度等于视口宽度，错误态无横向溢出，主题保留 | 浏览器移动视口不替代真实手机流量 |

阶段五浏览器验收不改变公开入口决定：本地修改未部署，手机流量、无痕浏览器和桌面线上交互仍需产品负责人本人验活。

## 2026-07-27 公网自动复核

| 检查 | 结果 | 证据边界 |
| --- | --- | --- |
| HTTPS `HEAD` | 200 | 只证明自动请求可达 |
| HTTPS `GET` | 200 | 只证明首页资源返回成功 |
| TLS 校验 | 通过，退出码为 0 | 只证明本次证书校验通过 |
| 托管响应 | Vercel 响应正常 | 不证明交互主路径可用 |
| 手机流量 | 待人工验证 | 当前执行环境无法替代真实手机网络 |
| 无痕浏览器 | 待人工验证 | 企业浏览器策略阻止本轮交互检查 |
| 桌面浏览器主路径 | 待人工验证 | 企业浏览器策略阻止本轮交互检查 |

自动复核通过不等于 Demo 已完成发布验收，也不作为恢复 README 在线入口的依据。

## 2026-07-21 自动检查

| 检查 | 结果 |
| --- | --- |
| HTTPS 请求 | 200 |
| 当前网络响应时间 | 约 1.6 秒 |
| HTTP 入口 | 308 重定向至 HTTPS |
| 无登录首页 | 已验证：首屏直接展示经典生成输入 |
| 经典生成主路径 | 已验证：主题输入后生成按钮从 disabled 变为 enabled；未提交模型请求 |
| 移动端 | 已验证：375px 视口无横向溢出，主题、平台、内容类型和高级选项可见 |
| 桌面端 | 已验证：双栏创作简报与候选区首屏可见 |

截图：

- [移动端首屏](assets/demo-mobile.png)
- [桌面端首屏](assets/demo-desktop.png)

## 人工三端复测

以下结果必须由产品负责人在投递前使用真实设备或浏览器填写；自动检查不能替代人工验活。

| 场景 | 日期 | 结果 | 异常与处理 |
| --- | --- | --- | --- |
| 手机流量访问 | 待人工填写 | 待验证 | 验收首页、经典生成主路径和错误态 |
| 无痕浏览器访问 | 待人工填写 | 待验证 | 验收无缓存、无登录状态下的首屏与主路径 |
| 桌面浏览器主路径 | 待人工填写 | 待验证 | 验收输入、候选生成、选择与反馈；不得提交真实敏感内容 |

三项均通过且无密钥、隐私或错误态问题时，才可恢复 README 与 GitHub Website 在线入口。任一场景失败或未完成时，招聘入口只使用 GitHub、截图和 60–90 秒录屏。

## 投递前检查

1. 使用手机流量、无痕浏览器和桌面浏览器分别访问。
2. 确认 30 秒内能理解产品并进入经典生成主路径。
3. 检查无 Key、上游失败、请求超限和移动端错误态。
4. 录制 60–90 秒备用演示：问题与用户 → 输入 → 10 个候选 → 选择/反馈 → 评测边界。
5. 地址不稳定时，简历优先使用 GitHub、截图和录屏，不放失效链接。
