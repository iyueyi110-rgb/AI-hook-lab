# 运营分析 Agent

运营分析 Agent 是管理后台中的只读证据查询入口。它可查询看板聚合、评测批次、Prompt 版本和 Bad Case，并要求数字结论关联数据来源与时间范围。

它不能修改 Prompt、发布版本、写入评测结果、发送消息或把模拟数据解释为真实用户效果。没有完成评测或真实事件时，应明确返回证据不足，而不是生成结论。

生产环境默认关闭，只有管理员且显式启用 `OPS_AGENT_ENABLED=true` 后可访问。实现与验收重点见 `lib/agent/ops-*` 及对应测试。
