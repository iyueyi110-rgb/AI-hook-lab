# 作品集验收与简历对齐记录

本记录用于把仓库可复核事实、待补证据和简历表述放在同一条验收链路中。它不新增测试数据，也不把方法实现改写成真实用户结果。

## 证据分级

| 级别 | 定义 | 当前内容 | 对外写法 |
| --- | --- | --- | --- |
| 已验证事实 | 仓库中存在可复核文件、代码、测试或可重复命令 | 20 个主题 × 3 个平台的 60 个固定案例；创作 Agent 与运营分析 Agent 的规则、测试和安全门禁 | 可以陈述实现与评测方法，同时说明不代表真实传播效果 |
| 方法已实现 | 流程、指标或决策规则已固化，但没有完整业务样本 | 人工盲评、第三人裁决、来源隔离、Bad Case 护栏与 Prompt 升级门槛 | 只能写“设计并实现方法”，不能写成已发生的提升或升级决定 |
| 未验证主张 | 缺少脱敏原始记录、整数分子分母、正式导出或可复算结果 | 简历中的受控测试人数、任务数、产出数、收藏比例、选择比例和离线提升值 | 从简历与公开结论中删除；补证后按实际计算结果恢复 |
| 人工待验收 | 自动检查通过，但真实设备或交互主路径未完成复核 | 在线 Demo 的手机流量、无痕浏览器与桌面交互 | 暂不作为公开入口 |

详细主张状态见 [简历主张审计](../evidence/claims-audit.md)，指标名称与分子分母见 [指标字典](../evidence/metrics-dictionary.md)。

## 简历待同步清单

以下数字在真实脱敏 CSV、计算脚本和口径报告齐备前，保持 `not_verified`：

- 目标创作者人数、受控任务次数与 Hook 产出数量。
- 收藏比例与测试内最终选择比例；不得把测试内选择写成真实发布采用。
- 离线可用率和平台适配率的提升值。
- 缺少逐条标签时的主要 Bad Case 分布结论。

恢复任何数字前，必须同时满足：

1. 原始记录已脱敏，且可以关联任务、Hook、Prompt 版本与 `dataOrigin`。
2. 报告明确分子、分母、排除条件、时间范围和样本量。
3. 仓库计算脚本能从原始记录复算结果，不从简历数字反推数据。
4. 由本人检查反例、Bad Case 和护栏，再决定是否同步简历。

## 2026-08-06 可复核验收记录

本轮使用从远程 `main` 恢复的有效 Git 工作树。以下结果均来自实际命令或本地浏览器复核；当前修改尚未部署，也没有新增真实用户数据。

| 验收项 | 命令或记录 | 实际结果 | 证据边界 |
| --- | --- | --- | --- |
| 中文文档门禁 | `npm run docs:check` | 退出码 0；37 个已跟踪 Markdown 通过 | 未跟踪新增文档另用同一检查器独立验证 |
| 自动化测试 | `npm test` | 327 通过、0 失败 | 验证代码规则，不代表真实用户效果 |
| Agent 离线验收 | `npm run eval:agent` | 95 通过、0 失败 | 只属于确定性夹具与离线测量 |
| 静态检查 | `npm run lint` | 退出码 0 | 不替代业务验收 |
| 生产构建 | `npm run build` | 退出码 0；20 个应用路由完成构建 | 只证明当前代码能够构建 |
| 密钥扫描 | `npm run security:scan` | 252 个已跟踪文件，0 命中 | 扫描不打印匹配值，未跟踪文件需在提交后复核 |
| 证据清单 | `npm run evidence:verify` | 1 条已验证，4 条继续 withheld | 方法项不等于结果已经发生 |
| 补丁格式 | `git diff --check` | 退出码 0 | 当前仅有换行风格提示，无空白错误 |
| 本地错误主路径 | Playwright，390px 与 1440px | 无横向溢出；无 Key 返回安全 503；重试保留输入 | 未调用真实模型，不代表成功生成路径通过 |
| 完整 Mock 评测 | 隔离临时数据与 Playwright | 120/120 生成任务、360/360 候选、120/120 正式结果、240/240 评分、60/60 定案 | 重复 Mock 评分只验证流程、权限与门禁，不属于用户研究 |
| 角色与报告门禁 | 管理员、评测员、裁决员浏览器复核 | 权限隔离通过；完成的 Mock 报告仍为“需要继续评测”；七个导出入口均返回 200 | 不证明 Live 模型质量或真实采用 |
| Demo 自动复核 | [Demo 验证记录](demo-verification.md) | 2026-07-27：HTTPS 自动请求 200、TLS 校验通过 | 不能证明当前未部署修改已上线，也不能替代三端人工验活 |

产品决策与未验证假设见[产品决策简报](product-decision-brief.md)。提交或部署后必须重新执行全部门禁；本表不预填线上部署和人工设备验收为通过。

招聘方结论、分项评分、演示顺序和建议简历表述见[产品经理 HR 最终评分卡](pm-hr-final-scorecard.md)。

## Demo 人工验活字段

| 场景 | 验收人 | 日期 | 结果 | 失败时处理 |
| --- | --- | --- | --- | --- |
| 手机流量 | 本人填写 | 待填写 | 待验证 | 不恢复在线入口，保留 GitHub、截图和录屏 |
| 无痕浏览器 | 本人填写 | 待填写 | 待验证 | 排查缓存、Cookie、鉴权和首屏依赖 |
| 桌面浏览器主路径 | 本人填写 | 待填写 | 待验证 | 记录复现步骤，修复后重新完成三端验收 |

任一项未完成或失败，README 与 GitHub Website 均不得将 Demo 标记为公开可用。
